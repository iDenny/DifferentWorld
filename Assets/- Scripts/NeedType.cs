using System;

// Enum listing the different basic needs a character can have.
// Extend this list to include new needs such as Hygiene or Entertainment.
public enum NeedType
{
    Hunger,
    Rest,
    Social,
    Recreation
}